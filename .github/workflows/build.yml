name: Build Surgical Vault IPA

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: "VaultCore -> target"

      - name: Setup Rust Targets
        run: |
          rustup target add aarch64-apple-ios

      - name: Build Rust Core
        run: |
          cd VaultCore
          cargo build --target aarch64-apple-ios --release
          mkdir -p ../VaultApp/Libs
          cp target/aarch64-apple-ios/release/libvault_core.a ../VaultApp/Libs/

      - name: Generate Xcode Project
        run: |
          brew install xcodegen
          xcodegen generate

      - name: Build iOS App
        run: |
          xcodebuild -project VaultApp.xcodeproj \
            -scheme VaultApp \
            -sdk iphoneos \
            -configuration Release \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Package IPA
        run: |
          mkdir -p Payload
          # The path below is derived from your -derivedDataPath 'build'
          cp -r build/Build/Products/Release-iphoneos/VaultApp.app Payload/
          zip -r VaultApp.ipa Payload/

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SurgicalVault-IPA
          path: VaultApp.ipa